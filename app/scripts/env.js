define( function() {
    var env = {
        images: '<%= imagePath %>/images/',
        assets: '<%= imagePath %>/',
        root:'../'

        //Add other properties here
    };
    return env;
});